[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boatrace-ai"
version = "0.2.0"
description = "BOAT RACE prediction and paper trading system"
requires-python = ">=3.12"
dynamic = ["dependencies"]

[project.scripts]
boatrace-pre-night = "boatrace_ai.cli.pre_night:main"
[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
